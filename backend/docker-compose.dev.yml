services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: velune
      RABBITMQ_DEFAULT_PASS: velune
    ports:
      - "5672:5672"
      - "15672:15672"
